[project]
name = "skill-bucket"
version = "0.1.0"
description = "AI system for continuous personal development"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.12.0",
    "pre-commit>=4.5.1",
]
test = [
    "pytest>=9.0.2",
]
